# Railway 部署配置
# 用于 Python FastAPI 应用

[build]
  builder = "DOCKERFILE"
  dockerfile = "Dockerfile"

[deploy]
  # Railway 自动注入 PORT 环境变量
  healthcheck_path = "/health"
  restart_policy = "on-failure"
  restart_policy_max_retries = 5
